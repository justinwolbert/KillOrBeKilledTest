//// obj_crate : Create
//sprite_index  = spr_crate_test;
////mask_index   = spr_crate_mask;   // <— keeps the tight box
//image_speed   = 0;          // idle frame only
//state         = 0;          // 0 = intact, 1 = breaking
//solid         = true;       // acts like a wall until broken
